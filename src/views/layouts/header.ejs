<!DOCTYPE html>
<html lang="pt-BR">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
		<title><%= pageInfo.seo.title %></title>
		<meta name="description" content="<%= pageInfo.seo.description %>">
		<link rel="canonical" href="<%= pageInfo.seo.siteUrl %><%= pageInfo.seo.canonical %>">
		<% if (pageInfo.pagination) { %><% if (pageInfo.pagination.currentPage > 1) { %><link rel="prev" href="<%= pageInfo.seo.siteUrl %><%= pageInfo.pagination.previousPage %>"><% } %>
		<% if (pageInfo.pagination.currentPage < pageInfo.pagination.totalPages) { %><link rel="next" href="<%= pageInfo.seo.siteUrl %><%= pageInfo.pagination.nextPage %>"><% } %><% } %>        
        <!-- Favicon-->
        <link rel="icon" type="image/x-icon" href="<%= pageInfo.seo.staticUrl %>/assets/favicon.ico" />
        <!-- Bootstrap icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css" rel="stylesheet" />
        <!-- Core theme CSS (includes Bootstrap)-->
        <link href="<%= pageInfo.seo.staticUrl %>/css/styles.css" rel="stylesheet" />
        <style>body {font-family: 'Roboto', sans-serif;}</style>
    </head>
    <body class="d-flex flex-column h-100">
        <main class="flex-shrink-0">
            <!-- Navigation-->
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
                <div class="container-fluid px-5">
                    <a class="navbar-brand" href="/">CEP Busca</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                            <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                            <li class="nav-item"><a class="nav-link" href="/estados">Estados</a></li>
                            <li class="nav-item"><a class="nav-link" href="/cidades">Cidades</a></li>
                            <li class="nav-item"><a class="nav-link" href="/ceps">CEP</a></li>
                            <li class="nav-item"><a class="nav-link" href="/conteudo/sobre">Sobre</a></li>
                            <li class="nav-item"><a class="nav-link" href="/conteudo/contato">Contato</a></li>
                        </ul>
                    </div>
                </div>
            </nav>
